apiVersion: v1
kind: Service
metadata:
  name: {{ include "capgemini-assignment.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "capgemini-assignment.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
